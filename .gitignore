# Tooling caches
.atl/
.engram/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.egg-info/

# Node
node_modules/
dist/
dist-e2e/
build/
coverage/
.vite/
*.local

# Playwright (frontend/e2e/)
playwright-report/
test-results/

# One-off real-data validation runs against a scratch DB/backend (never `energia`) -- see
# frontend/README.md, "Validación manual con datos reales". Throwaway output, not part of the
# app or the automated test suite.
frontend/scratch-validation/

# Environment
.env
.env.*
# plantilla versionada: env.example (sin punto inicial)

# IDE / OS
.vscode/
.idea/
.DS_Store

# Data (keep samples only, never raw datasets)
# Not root-anchored: the synthetic generator's own README has always documented running it from
# backend/ (`cd backend && make seed-synthetic ...`), which writes to backend/datasets/synthetic/
# -- a root-anchored `datasets/synthetic/` never matched that real output path.
datasets/raw/
**/datasets/synthetic/
*.parquet
*.sqlite

# ML artifacts
*.pkl
*.joblib
mlruns/

# Datos reales de la distribuidora (PII: titular, CUIT, coordenadas de domicilios).
# NUNCA versionar: el repositorio es público. Guardar en datasets/raw/ (ya ignorado) si se necesita.
/*.csv
