"""Bounded context: Gestión de Clientes (DOMAIN_MODEL.md §4.1).

First bounded context implemented (US-001). See `contexts/README.md` for the naming
convention this package follows (Spanish domain nouns, English technical parts) and the
source-port pattern used by the import use case.
"""
